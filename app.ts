let hello = (name: string, nickname: string) => {
  console.log('Hello ' + name + 'Nick: ' + nickname)
}

hello('William', 'Will I  am')


let mensagem: string = 'seja bem vindo'
let tempo: number = 20
let statusCode: boolean = true
let listaFrutas: Array<string> = ['banana', 'maça', 'manga']
let notas: Array<number> = [1,2,3,4]
let notasFinais: number[] = [1, 2, 3, 10]

/* 

comando: tsc -init , vai criar o arquivo tsconfig.json

tsconfig.json

outDir -> diretorio onde os arquivos compilados serão direcionados

*/