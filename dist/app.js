"use strict";
var hello = function (name, nickname) {
    console.log('Hello ' + name + 'Nick: ' + nickname);
};
hello('William', 'Will I  am');
var mensagem = 'seja bem vindo';
var tempo = 20;
var statusCode = true;
var listaFrutas = ['banana', 'maça', 'manga'];
var notas = [1, 2, 3, 4];
var notasFinais = [1, 2, 3, 10];
/*

comando: tsc -init , vai criar o arquivo tsconfig.json

tsconfig.json

outDir -> diretorio onde os arquivos compilados serão direcionados

*/ 
